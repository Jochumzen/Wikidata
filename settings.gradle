pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Wikidata"
include ':app'
include ':wikidata_datasource'
include ':domain'
project(':domain').projectDir = new File('/../MapiGanja/domain')
